# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

#  THIS FOR HOSTED DATABASE
DATABASE_URL="mysql://sale:2002afidhu@127.0.0.1:3306/saledb"
DATABASE_USER="sale"
DATABASE_PASSWORD="2002afidhu"
DATABASE_NAME="saledb"
DATABASE_HOST="127.0.0.1"
DATABASE_PORT=3306
PORT=3000

# THIS FOR LOCAL DATABASE
# DATABASE_URL="mysql://sale:2002afidhu@127.0.0.1:3306/saledb"


# DATABASE_URL="mysql://root:2002afidhu@127.0.0.1:3306/oweruSales"
# DATABASE_USER="root"
# DATABASE_PASSWORD="2002afidhu"
# DATABASE_NAME="oweruSales"
# DATABASE_HOST="127.0.0.1"
# DATABASE_PORT=3306
# PORT=3000